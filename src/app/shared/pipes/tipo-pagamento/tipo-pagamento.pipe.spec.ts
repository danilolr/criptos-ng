import { TipoPagamentoPipe } from './tipo-pagamento.pipe';

describe('TipoPagamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPagamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
