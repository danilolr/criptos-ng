import { ValidoPagamentoPipe } from './valido-pagamento.pipe';

describe('ValidoPagamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidoPagamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
